export class EmailModel {
    public debug: boolean = false;
    public user: string;
    public password: string;
    public host: string;
    public port: number;
    public secureProtocol: string;
}